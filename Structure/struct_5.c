#include <stdio.h>
 struct abc{
        int a;
        int b;
    };
int main(){
   
    struct abc *ptr;
    printf("Enter two numbers:");
scanf("%d %d",&ptr->a,&ptr->b);
printf("a = %d  b = %d sum = %d",ptr -> a,ptr->b,ptr->a + ptr->b);

         
    return 0;
}